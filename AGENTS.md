# Agents

## requirements

+ 本项目目前处于开发阶段。所有改动都应彻底完成，优先直接调整到目标设计与行为，不为旧实现保留兼容层、兼容分支或迁移包袱。
+ 本项目目前处于开发阶段。数据库表结构改动不要产生新的 migrate，直接修改 baseline 的，数据可以重置。
+ 项目尽可能使用框架提供的UI组件，例如 shadcn/ui 的，尽量减少/不用原生UI组件。
+ 新增功能、修改问题、重构，都要彻底，不要最小化，要用最优方案。
+ 项目是独立开发，功能模块开发不用开新分支，直接在 main 上开发。

## configs

+ 默认的本地测试账号是 ricl@hyperits.com 密码是 12345678
